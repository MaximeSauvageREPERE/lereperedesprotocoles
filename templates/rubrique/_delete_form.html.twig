<form method="post" action="{{ path('app_rubrique_delete', {'id': rubrique.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette rubrique ?');" style="display:inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rubrique.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
