{% extends 'base.html.twig' %}

{% block title %}Nouveau Domaine{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-3 p-3">
        <h1 class="mb-0">Créer un nouveau domaine</h1>
        <a href="{{ path('app_domaine_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>
    <div class="card shadow-sm mb-4 bg-light">
        <div class="card-body">
            {{ include('domaine/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>
        <div class="card-footer d-flex justify-content-end gap-2 bg-light">
            <button type="submit" form="domaine_form" class="btn btn-outline-primary">{{ 'Créer' }}</button>
        </div>
    </div>
{% endblock %}
